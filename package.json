{
  "name": "strong-arc-project-list",
  "description": "Arc backend component to store a list of projects",
  "version": "4.0.0",
  "main": "server/server.js",
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore .",
    "test": "tap --bail --coverage --coverage-report=cobertura --timeout=200 test/test-*.*",
    "posttest": "nyc report --reporter=lcov && nyc report"
  },
  "dependencies": {
    "compression": "^1.0.3",
    "cors": "^2.5.2",
    "loopback": "^3.x",
    "loopback-boot": "^2.6.0",
    "mkdirp": "^0.5.1",
    "osenv": "^0.1.3",
    "semver": "^5.3.0",
    "serve-favicon": "^2.0.1",
    "strong-error-handler": "^1.x"
  },
  "optionalDependencies": {
    "loopback-component-explorer": "^6.4.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-strongloop": "^2.x",
    "mktmpdir": "^0.1.1",
    "nyc": "^14.1.1",
    "supertest": "^4.0.2",
    "tap": "^14.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/strongloop/strong-arc-project-list.git"
  },
  "engines": {
    "node": ">=8"
  },
  "bugs": "https://github.com/strongloop/strong-arc-project-list/issues",
  "keywords": [
    "arc",
    "StrongLoop"
  ],
  "license": "SEE LICENSE IN LICENSE.txt",
  "author": "engineering@strongloop.com",
  "homepage": "https://github.com/strongloop/strong-arc-project-list"
}
